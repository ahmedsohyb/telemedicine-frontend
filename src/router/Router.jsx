import React from 'react'
import { BrowserRouter, Route } from 'react-router'

const Router = () => {
  return (
    <BrowserRouter>
        <Routes>
            
        </Routes>
    </BrowserRouter>
  )
}

export default Router